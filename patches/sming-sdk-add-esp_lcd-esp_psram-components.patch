diff --git a/Sming/Arch/Esp32/Components/esp32/component.mk b/Sming/Arch/Esp32/Components/esp32/component.mk
index 66ed206c..671fefaf 100644
--- a/Sming/Arch/Esp32/Components/esp32/component.mk
+++ b/Sming/Arch/Esp32/Components/esp32/component.mk
@@ -311,6 +311,10 @@ else
 SDK_COMPONENTS += xtensa
 endif
 
+# esp_lcd and esp_psram are required by RGB-panel display drivers (e.g. ST7701).
+# They are standard IDF components but not pulled in by default.
+SDK_COMPONENTS += esp_lcd esp_psram
+
 SDK_ESP_WIFI_LIBS := \
 	coexist \
 	core \
